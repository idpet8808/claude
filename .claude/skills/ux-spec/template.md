# UX 화면 명세서 — {{프로젝트명}}

- 고객사: {{고객사}}
- 슬러그: `{{slug}}`
- 작성자: UX기획자
- 작성일: {{YYYY-MM-DD}}
- 참조:
  - `01-prd.md` (버전 {{vX.Y}})
  - `02-tech-review.md` (버전 {{vX.Y}})

---

## 1. 화면 목록

| # | 화면명 | 진입 경로 | 주요 컴포넌트 | 연관 PRD Must |
|---|--------|-----------|--------------|--------------|
| S1 | {{홈}} | `/` | 헤더, 히어로, CTA | `01-prd.md §4 Must-1` |
| S2 | {{로그인}} | `/login` | 폼, 소셜 버튼 | `01-prd.md §4 Must-2` |

## 2. 화면 명세 (화면별)

### S1. {{화면명}}

**사용자 목표**: {{이 화면에서 사용자가 달성하고 싶은 것}}

**진입 경로**:
- {{경로 1}}
- {{경로 2}}

**영역 구성**:

| 영역 | 역할 | 포함 컴포넌트 |
|------|------|--------------|
| 헤더 | | |
| 메인 | | |
| 푸터 | | |

**4가지 상태** (모두 필수):

#### 정상 상태
{{데이터가 있고 사용자가 정상 흐름으로 진입했을 때}}

#### 빈 상태 (Empty)
{{데이터가 없을 때 — "아직 등록된 항목이 없습니다" + CTA}}

#### 에러 상태 (Error)
{{API 실패·권한 없음·네트워크 오류 — 사용자에게 무엇이 잘못됐는지 + 복구 경로}}

#### 로딩 상태 (Loading)
{{데이터 로딩 중 — 스켈레톤·스피너·프로그레스}}

**인터랙션**:

| 트리거 | 동작 | 결과 상태 |
|--------|------|----------|
| CTA 버튼 클릭 | API 호출 | 성공 → S2 / 실패 → 에러 토스트 |

**기술 제약 반영**:
- 기술검토자 `02-tech-review.md §2`에서 "조건부"로 분류된 {{기능}} → {{대안 경로}}로 대체

---

## 3. 공통 컴포넌트

| 컴포넌트 | 용도 | 사용 화면 |
|----------|------|----------|
| 헤더 | | S1, S2, ... |
| 토스트 | 피드백 알림 | 전체 |

## 4. 네비게이션 플로우

```
S1(홈) → [CTA] → S2(로그인) → [성공] → S3(대시보드)
                       ↓ [실패]
                       에러 상태 → 재시도 링크
```

## 5. 접근성·반응형 주요 결정

- 최소 터치 영역: 44×44px
- 색 대비: WCAG AA 이상
- 반응형: {{모바일 → 태블릿 → 데스크톱 breakpoints}}

## 6. 오픈 이슈

| # | 항목 | 담당자 | 기한 |
|---|------|--------|------|
| 1 | | | |

---

## 자가 점검

- [ ] PRD Must 수와 화면 명세의 주요 플로우 수 정합
- [ ] 모든 화면에 빈·에러·로딩 상태 4가지 모두 정의됨
- [ ] 모든 화면에 진입 경로 명시
- [ ] 기술검토 "조건부/불가능" 항목이 화면에서 올바르게 처리됨(대체 또는 제외)

**통과율**: N/4
